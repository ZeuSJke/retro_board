export const metadata = {
  title: 'Admin - RetroBoard',
  description: 'Управление пространствами RetroBoard',
}

export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
